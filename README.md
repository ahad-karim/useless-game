Download everything as zip.  
Extract the zip.  
create a new Visual Studio 13 project named "LevelDevil"  ----> The name is very important.  
copy everyting and paste it in the "Source" folder of your project(if asked to replace any file, replace them).  
Build and run.  
If you get any error, at the top left, click "Build" -> "Clean Solution", then "Build" -> "Rebuild Solution", then try running it.  

